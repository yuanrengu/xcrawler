# Security Policy

## 支持范围

当前项目仍处于早期工程化阶段，安全修复优先覆盖最新 `main` 分支。历史版本不承诺单独维护。

## 报告安全问题

如果你发现以下问题，请不要直接公开披露可利用细节：

- API key、token、`.env` 或缓存数据泄露风险
- 可导致非预期读取、覆盖本地文件的问题
- 会绕过隐私默认保护的行为
- 报告中暴露敏感生活事件、联系方式、地址或原文证据的问题
- 依赖或工作流中的高危供应链风险

请通过 GitHub Security Advisories 私下报告；如果不可用，请创建一个不含可利用细节的 issue，请维护者联系你。

## 密钥与本地数据

xcrawler 会在本地 `cache/`、`cache_backup/` 和报告文件中保存抓取结果、翻译结果、分析结果和运行记录。JSON 覆盖写入还会保留同名 `.bak` 恢复文件。在 POSIX 系统上，新建缓存/导出目录默认使用 `0700`，受管 JSON、备份、SQLite、CSV、HTML 和 PNG 文件默认使用 `0600`；Windows 上为尽力保护，仍应依赖系统 ACL。

为避免意外改变用户已有目录，xcrawler 不会自动收紧预先存在的父目录；检测到组或其他用户可访问时会给出警告。显式指定的缓存根目录可以是符号链接，但根目录内的受管文件、备份、JSON `.lock` 文件和 SQLite sidecar 不得是符号链接；锁文件也不得是硬链接或在打开期间被替换。存储键不得包含绝对路径或目录穿越片段。

JSON 操作通过同名 `.lock` 文件使用跨进程 advisory lock，默认等待上限为 5 秒。锁文件本身在 POSIX 上使用 `0600`，并会保留供后续运行复用；不要在程序运行期间删除或替换它。进程异常退出时操作系统会释放锁，不需要把残留的空锁文件视为“死锁”。

建议：

- 不提交 `.env`、`cache/`、`cache_backup/` 或生成的报告。
- 收到目录权限警告时，确认目录不被其他账号共享后手动执行 `chmod 700 <目录>`；分享前不要仅依赖本地文件权限。
- 为公开演示使用专门的测试账号和低权限 API key。
- 定期轮换 X/Twitter、DeepSeek、OpenAI 等 API key。
- CSV 导出会转义电子表格公式前缀并把 Tweet ID 强制按文本保存，但分享 HTML、Markdown、CSV 或 JSON 报告前仍应检查敏感事件和证据原文。

## 隐私默认值

项目默认只应分析公开内容，并且：

- 行为分析默认隐藏敏感生活事件详情和证据 tweet id。
- HTML 报告默认隐藏敏感事件证据原文。
- 邮箱、电话号码、地址类文本会做基础脱敏。
- 如确需完整敏感事件证据，必须显式启用相关参数。

任何改变这些默认值的 PR 都应被视为安全和隐私敏感变更。
