# TARGET_DATE 配置示例

## 📅 目标日期配置说明

通过 `.env` 文件中的 `TARGET_DATE` 配置，你可以灵活设置增量抓取的目标日期。

## 🎯 常用配置示例

### 1. 抓取到2024年初（默认）
```bash
TARGET_DATE=2024-01-01
```
适用场景：获取用户2024年及之前的完整历史数据

### 2. 抓取到2023年初
```bash
TARGET_DATE=2023-01-01
```
适用场景：获取更长的历史数据，包含2023年及之前

### 3. 抓取到2025年初
```bash
TARGET_DATE=2025-01-01
```
适用场景：仅获取最近的数据，适合新用户或测试

### 4. 抓取到特定日期
```bash
TARGET_DATE=2023-06-15
```
适用场景：获取到特定事件日期的历史数据

## ⚙️ 使用方法

1. **修改配置**：编辑 `.env` 文件中的 `TARGET_DATE`
2. **运行抓取**：`./refetch_data.sh -i` 或 `python3 fetch_more_history.py`
3. **自动停止**：脚本会在达到目标日期或最早推文时自动停止

## 🔍 工作原理

- **智能判断**：如果最早推文 ≤ 目标日期，停止抓取
- **灵活适应**：如果用户在目标日期前没有推文，抓取到最早推文
- **格式验证**：自动验证日期格式，错误时使用默认值

## 💡 最佳实践

- **首次使用**：建议使用默认的 `2024-01-01`
- **深度分析**：可设置为 `2023-01-01` 获取更多历史数据
- **快速测试**：可设置为较近的日期，如 `2024-06-01`
- **特定研究**：根据研究需求设置特定的历史节点

## ⚠️ 注意事项

- 日期格式必须为 `YYYY-MM-DD`
- 设置过早的日期可能导致抓取时间过长
- Free API 用户建议配合 `MAX_PAGES=10` 使用