# 增量抓取与数据扩展指南

## 🔍 何时需要增量抓取

当你发现现有数据量较少或时间范围不够覆盖时，可以使用增量抓取功能补充数据。

## 🚀 方案一：使用增量抓取脚本（推荐，不覆盖现有数据）

### 运行增量脚本

```bash
xcrawler fetch-more
```

该脚本会：
1. 加载现有的数据文件
2. **抓取最新**：自动获取上次抓取后新发布的所有推文
3. **补全历史**：从最早的推文继续向历史抓取（直到 `TARGET_DATE`）
4. 自动去重并合并数据

### 配置目标日期

在 `.env` 文件中设置：

```
TARGET_DATE=2024-01-01
```

脚本会抓取到该日期为止。如需更早的数据，修改此值为更早的日期。

---

## 📊 方案二：增加主抓取页数

在 `.env` 中通过 CLI 参数调整（无需修改代码）：

```bash
# 增加页数以获取更多推文（每页 100 条）
xcrawler fetch --pages 30
```

或通过统一 CLI：

```bash
xcrawler fetch --pages 30
```

页面数与预估数据量：

| 页数 | 预估最大推文数 | 建议场景 |
|------|-------------|---------|
| 10 | 1000 条 | 日常更新 |
| 20 | 2000 条 | 轻度用户 |
| 30 | 3000 条 | 推荐默认 |
| 50 | 5000 条 | 超活跃用户 |

---

## ⚠️ API 限制注意

Twitter API 有严格限制：

| 限制类型 | 数值 |
|---------|------|
| 每 15 分钟 | 1500 请求 |
| 每页推文数 | 100 条 |
| 建议间隔 | 3 秒/请求 |

**抓取耗时估算**：
- 1000 条（10 页）≈ 30 秒
- 5000 条（50 页）≈ 150 秒
- 如果触发限流，需等待 15 分钟

---

## 🔧 故障排除

### 问题 1: API 限流 (429)

脚本会自动读取限流重置时间并等待。等待时间过长时会自动跳过，不会卡死。

### 问题 2: 数据文件损坏

如果 `cache/{username}_raw_tweets.json` 损坏，脚本会自动：
1. 创建 `.bak` 备份
2. 从头开始抓取

---

## 📝 抓取后续步骤

数据更新后，需要重新翻译和分析：

```bash
# 1. 翻译新推文（自动增量，跳过已翻译内容）
xcrawler translate

# 2. 重新进行各项分析
xcrawler analyze behavior
xcrawler analyze interest
```

---

## 💡 优化建议

### 使用翻译缓存

已翻译的内容会被缓存在 `cache/translation_cache.json`，下次运行自动跳过，节省 API 调用。

### 备份数据

```bash
# 抓取前备份
cp -r cache cache_backup

# 如果出错可以恢复
cp cache_backup/* cache/
```

---

## 🎯 推荐流程

**日常更新**（保留现有数据，补充新推文）：

```bash
xcrawler fetch-more
xcrawler translate
xcrawler analyze behavior
```

**首次全量抓取**：

```bash
xcrawler fetch --pages 30
```

---

*注意: 抓取大量历史数据会消耗更多 API 配额和时间，请合理安排。*
